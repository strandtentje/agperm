{
	instances = {
		check = DataReader(
			f"queries/GetUserResourceActionAccess.sql", 
			connection = "agperm"
		) {
			none->Return("denied");
			last->Return("granted");
		};

		checksub = DataReader(
			f"queries/GetUserSubresourceActionAccess.sql",
			connection = "agperm"
		) {
			none->Return("denied");
			last->Return("granted");
		};
	};
}